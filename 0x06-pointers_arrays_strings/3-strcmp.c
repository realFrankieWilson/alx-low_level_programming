#include "main.h"

/**
 * _strcmp -> a function that compares two strings
 * @s1: stirng one
 * @s2: string two
 *
 * Return: 0;
 */

int _strcmp(char *s1, char *s2)
{
	int x;

	x = _strcmp(s1, s2);
	return (x);
}
