#include "main.h"
/**
 * _abs -> returns the absolute number of an int.
 * @s: a parameter
 *
 * Return: Always 0 (Success)
 */
int _abs(int s)
{
	if (s < 0)
		return (-s);
	else if (s == 0)
		return (s);
	else
		return (s);
	return (0);
}
